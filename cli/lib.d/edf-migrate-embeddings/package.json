{
  "name": "edf-migrate-embeddings",
  "version": "1.0.0",
  "description": "edfMigrateEmbeddings — one-shot migration of a golden manifest's inline base64 embeddings into per-standard content-addressed sidecar vector stores (embedding-sidecar PLAN Phase 5). -census dry-runs the collision scan; -migrate mints the new manifest generation. Old blocks/manifests retained.",
  "main": "edfMigrateEmbeddings.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "TQ White II",
  "license": "ISC"
}
