# parserDescriptor.ini — toyForge self-description (Profile §3). The FIXTURE forge of the Forge
# Framework's hermetic unit-gate suite (lib/forge-framework/test/). It is NOT under forges/ and the
# forger never discovers it; the [parserDescriptor] section header is required because
# qtools-config-file-processor drops sectionless keys.
[parserDescriptor]
standardName=Toy
displayName=Toy Standard (fixture)
entryModule=forgeToy.js
roundTripValidator=roundTripValidator.js
defaultSnapshot=01
