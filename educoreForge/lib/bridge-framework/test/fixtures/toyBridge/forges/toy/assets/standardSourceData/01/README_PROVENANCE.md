# Toy Bridge fixture snapshot 01

A hand-written toy crosswalk (property tier) and a toy descriptor list (value tier) for the Bridge Framework's
hermetic gate suite. No licensed bytes. Immutable: a gate that needs different bytes copies to os.tmpdir().
