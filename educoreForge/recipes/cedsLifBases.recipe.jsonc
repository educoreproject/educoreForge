{
  // cedsLifBases — CEDS and LIF base graphs only, no hub and no bridge.
  // The full cedsLif recipe exercises hubs + bridges, both of which are deferred work as of
  // 2026-07-23 (bridgeMaker is stub-bodied; the hub add hits the standardName@version key
  // collision recorded as Decision 1). This recipe reforges just the two standardBase graphs —
  // the forge -> init -> harvest -> manifest -> materialize spine — which completes cleanly today.
  "schemaVersion": "1.0.0",
  "recipeName": "cedsLifBases",
  "kind": "dev",
  "description": "CEDS + LIF standardBase graphs only (no hub, no bridge).",
  "standards": [
    { "token": "ceds", "version": "current" },
    { "token": "lif", "version": "current" }
  ],
  "hubs": [],
  "bridges": []
}
