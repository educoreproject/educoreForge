{
  // pescOnlyRoundTrip - pescOnly PLUS the RT-13 round-trip stage (forge-edfi Phase 4 proof
  // recipe): forge pesc alone, materialize, then run its DECLARED roundTripValidator against
  // the product graph and land the verdict with the build outputs. The stage opt-in below is
  // the doctrine §7.4 recipe choice; pescOnly.recipe.jsonc remains the stage-off sibling.
  "schemaVersion": "1.0.0",
  "recipeName": "pescOnlyRoundTrip",
  "kind": "dev",
  "description": "Forge the pesc standard base and round-trip-validate the materialized product (RT-13 stage proof).",
  "roundTripStage": true,
  "standards": [ { "token": "pesc", "version": "current" } ],
  "hubs": [],
  "bridges": []
}
