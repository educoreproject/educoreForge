{
  // ctdlOnly — a minimal NEW-FORMAT build recipe: forge CTDL alone, no hubs, no bridges.
  // PLAN P1 (port the CTDL forge bundle): proves the ported forges/ctdl/ bundle self-registers,
  // resolves through the forger, and forges to its standardBase graph — the smallest recipe that
  // exercises CTDL. The bridge to the CEDS hub is later work (P2 authored / P3 inferred); this
  // recipe carries no hub and no bridge, exactly like lifOnly.
  "schemaVersion": "1.0.0",
  "recipeName": "ctdlOnly",
  "kind": "dev",
  "description": "Forge the CTDL standard base only — proves the ported CTDL bundle forges.",
  "standards": [
    { "token": "ctdl", "version": "current" }
  ],
  "hubs": [],
  "bridges": []
}
