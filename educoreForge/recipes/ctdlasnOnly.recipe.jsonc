{
  // ctdlasnOnly — a minimal NEW-FORMAT build recipe: forge CTDL-ASN alone, no hubs, no bridges.
  // Proves the ported forges/ctdlasn/ bundle self-registers (parserDescriptor), resolves through the
  // forger, forges to its standardBase graph, composes a 1-member manifest and materializes — the
  // smallest recipe that exercises CTDL-ASN. CTDL-ASN carries ZERO CEDS mappings and is forge-only:
  // it is an ISLAND at the forge layer (impliedTargets []), so this recipe carries NO hub and NO
  // bridge, exactly like ctdlOnly / lifOnly. The ceterms overlap and qdata references it stashes as
  // crossRefs are bridge material for a separate additive phase, never authored here.
  "schemaVersion": "1.0.0",
  "recipeName": "ctdlasnOnly",
  "kind": "dev",
  "description": "Forge the CTDL-ASN standard base only — proves the ported CTDL-ASN bundle forges.",
  "standards": [
    { "token": "ctdlasn", "version": "current" }
  ],
  "hubs": [],
  "bridges": []
}
