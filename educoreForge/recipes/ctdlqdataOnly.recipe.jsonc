{
  // ctdlqdataOnly — a minimal NEW-FORMAT build recipe: forge CTDL-QData alone, no hubs, no bridges.
  // Proves the ported forges/ctdlqdata/ bundle self-registers (parserDescriptor.ini), resolves through
  // the forger, and forges to its standardBase graph — the smallest recipe that exercises CTDL-QData.
  // CTDL-QData carries ZERO CEDS mappings; it is FORGE-ONLY, so this recipe carries no hub and no
  // bridge, exactly like ctdlOnly / lifOnly.
  "schemaVersion": "1.0.0",
  "recipeName": "ctdlqdataOnly",
  "kind": "dev",
  "description": "Forge the CTDL-QData standard base only — proves the ported CTDL-QData bundle forges.",
  "standards": [
    { "token": "ctdlqdata", "version": "current" }
  ],
  "hubs": [],
  "bridges": []
}
