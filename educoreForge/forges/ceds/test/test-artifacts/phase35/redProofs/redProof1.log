test-cedsRoundTripValidator

THE REGISTRY IS EMPTY BY RULING — and the emptiness must be load bearing, not decorative

THE A13 PARTITION IS PERFORMED ARITHMETIC — driven with a NONZERO omitted count, the state no CEDS fixture reaches
  FAIL populated: explicitlyOmittedTotal is 40 — NONZERO, which is the whole point of this section
       expected 40, got 0
  FAIL populated: contentGapTotal drops to the 10 genuine gaps
       expected 10, got 50
  FAIL populated: lostTotal follows contentGap, not the sum
       expected 10, got 50
  FAIL CONTENT GAP and NOT REPRODUCED are DIFFERENT numbers here — the partition can discriminate the two arithmetics
       contentGap=50 notReproduced=50
  FAIL the shipped and populated runs disagree — so the registry parameter is load bearing, not ignored
       shipped=50 populated=50
  FAIL populated: the matched rows are reported by name, not just counted
       expected "[{\"predicate\":\"https://w3id.org/CEDStandards/terms/chosenNotToCarry\",\"explicitlyOmitted\":40}]", got "[]"
  FAIL claiming EVERY lost predicate flips roundTripClean to true over 50 not-reproduced statements
       {"roundTripClean":false,"reproduced":100,"lostTotal":50,"contentGapTotal":50,"explicitlyOmittedTotal":0,"notReproducedTotal":50,"inventedTotal":0,"explicitlyOmittedRowList":[]}

THE PARTITION INVARIANT REFUSES BY NAME — a partition that does not close is not a verdict

RT-3 AT THE DOOR — five separate wrongs, five separate refusals, each by name

THE UNIFORM ENTRY POINT REFUSES BY NAME — no default graph, no default output

THE ANSWER KEY — the full graph writes its own checksummed snapshot

END TO END — a REAL verdict from the real code path, adjudicated by the LIVE RT-13 stage

LOSS IS DETECTED — the clean reading above is a result, not a property of the rig

test-cedsRoundTripValidator: FAILURES
  - populated: explicitlyOmittedTotal is 40 — NONZERO, which is the whole point of this section
  - populated: contentGapTotal drops to the 10 genuine gaps
  - populated: lostTotal follows contentGap, not the sum
  - CONTENT GAP and NOT REPRODUCED are DIFFERENT numbers here — the partition can discriminate the two arithmetics
  - the shipped and populated runs disagree — so the registry parameter is load bearing, not ignored
  - populated: the matched rows are reported by name, not just counted
  - claiming EVERY lost predicate flips roundTripClean to true over 50 not-reproduced statements
test-cedsRoundTripValidator: 68/75 passed, 7 failed.
