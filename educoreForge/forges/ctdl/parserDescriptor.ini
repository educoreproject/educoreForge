# parserDescriptor.ini — forge-ctdl self-description (BINDING spec §3.5, pairwiseVersionSwitching).
# This bundle IS its own registration: the discovery loader composes the roster from these
# descriptors; the central standard-registry is deleted. The [parserDescriptor] section header
# is required — qtools-config-file-processor drops sectionless keys.
[parserDescriptor]
standardName=CTDL
displayName=Credential Transparency Description Language (CTDL)
entryModule=forgeCtdl.js
# defaultSnapshot is an explicit pin (spec §15-6): dropping a new snapshot changes NOTHING until this line says so.
defaultSnapshot=01
# source is a specific FILE within the snapshot directory (file-bound parser). The recreation's
# forger resolveBundle requires sourceFile to resolve the bundle's defaultSource (the incumbent
# passed the snapshot DIRECTORY; the parser accepts either — directory or file — and finds
# ctdl-schema.json inside). Added to match the ceds/lif descriptors in the new tree.
sourceFile=ctdl-schema.json
