{
  // goldenRecipe — a golden IS a LIST of forgeRecipes (standard versions + pairing
  // selections + references), composed EXPLICIT-MEMBER (S14). This recipe documents
  // the DEPLOYED PRODUCTION golden GOLD_260718 (manifest c6607f74) — the modern
  // CTDL-family all-standards set carrying the CTDL->CEDS mapping campaign.
  //
  // DECLARATIVE ONLY. The recipe->manifest runner is not yet built, so this recipe
  // CANNOT be executed to reproduce c6607f74 today. It records the INTENDED / ACTUAL
  // composition of the golden as verified against the canonical forgeStore
  // (manifestBlocks JOIN blocks, manifestKey c6607f74, 40 members). It is the
  // authoritative declaration of what shipped, not a build script.
  //
  // LINEAGE: extends allStandards-260717 (manifest 81627d17 / GOLD_EVAL_260717).
  // The ONLY compositional delta 260717 -> 260718 is +5 mapping members: the five
  // CTDL->CEDS campaign blocks folded onto the pre-existing CEDS::CTDL pairing
  // (35 members -> 40 members).
  "recipeKind": "golden",
  "goldenName": "allStandards-260718",
  "standards": [
    "case", "ceds", "cip", "clr", "ctdl", "ctdlasn", "ctdlqdata", "dctap",
    "edfi", "eduapi", "jedx", "lif", "medbiquitous", "openbadges", "pesc",
    "sedm", "sif", "soc"
  ],
  "crosswalks": [
    "CEDS__CASE", "CEDS__CLR", "CEDS__CTDL", "CEDS__EdFi", "CEDS__EduAPI",
    "CEDS__JEDx", "CEDS__LIF", "CEDS__MedBiquitous", "CEDS__OpenBadges",
    "CEDS__PESC", "CEDS__SEDM", "CEDS__SIF", "CIP__SOC",
    "CTDL__CTDLASN", "CTDL__CTDLQData", "CTDLASN__CTDLQData"
  ],
  "references": [
    { "kind": "hubReference", "standard": "ceds", "hubVersion": "14.0.0.0" }
  ],
  // The distinguishing feature of 260718: the CEDS__CTDL pairing resolves to SIX
  // mapping blocks — the ONE legacy preserved EXACT block carried from 260717 PLUS
  // the FIVE CTDL->CEDS campaign blocks added this generation. All are pair-scoped
  // CEDS::CTDL (01,01) selections; enumerated here for fidelity since the runner
  // cannot yet re-mint them.
  "ctdlCedsMapping": {
    "pairSubject": "CEDS::CTDL",
    "versionKey": "(01,01)",
    "preservedFrom260717": [
      { "blockIdPrefix": "f957b88c", "producedBy": "edf-rekey",
        "tierScope": "value", "edges": { "EXACT_MATCH": 24 },
        "note": "Legacy preserved CEDS<->CTDL mapping; satisfies the 260717 preservation gate {EXACT_MATCH:24}." }
    ],
    "campaignSelections260718": [
      { "blockIdPrefix": "afce0f5d", "producedBy": "ctdlCedsCampaign-071726",
        "tierScope": "value", "edges": { "NARROW_MATCH": 22, "RELATED_MATCH": 5 },
        "note": "27 NARROW/RELATED spec block (adjudicated specialization mappings)." },
      { "blockIdPrefix": "a7a2926f", "producedBy": "ctdlCedsCampaign-071726",
        "tierScope": "property", "edges": { "EXACT_MATCH": 2 },
        "note": "2 element EXACT block (property-tier exact element matches)." },
      { "blockIdPrefix": "78fbd1b6", "producedBy": "edf-inferred",
        "tierScope": "property", "edges": { "CLOSE_MATCH": 113 },
        "note": "113 property CLOSE block (inferred close property mappings)." },
      { "blockIdPrefix": "4558ab66", "producedBy": "edf-inferred",
        "tierScope": "value", "edges": { "CLOSE_MATCH": 33 },
        "note": "33 value CLOSE block (inferred close value mappings)." },
      { "blockIdPrefix": "eac70457", "producedBy": "edf-inferred",
        "tierScope": "class", "edges": {},
        "note": "Class decision block — honest-empty (zero edges); records the class-tier adjudication that no class-level mapping was authored." }
    ]
  },
  "composition": {
    "mode": "explicitMember",
    "excludedLegacyCarriers": [
      "old CTDL standard block (superseded by the modern forge)",
      "ctdlFamilyUriBridge single bridge block (superseded by the 3 structural pairings)",
      "consolidated relationships block (retired: S5, residue-census ZERO OTHER)"
    ],
    "gates": [
      "membership assertion (zero legacy carrier ids)",
      "full-scale census (S9 three-way, per-edge provenance)",
      "preservation (CTDL->CEDS {EXACT_MATCH:24} legacy block + 26 cedsId anchors carried intact from 260717)",
      "CTDL->CEDS campaign census (5 added blocks: 27 NARROW/RELATED + 2 EXACT + 113 CLOSE property + 33 CLOSE value + honest-empty class decision)",
      "zero-NULL-provenance family census",
      "delta-closure (zero NEW violations vs base 260717)",
      "determinism (sequential two-replay fingerprint)",
      "purity XOR", "clean-room rebuild", "canonical byte-identity"
    ]
  },
  "notes": "40 members as built (verified against forgeStore manifestBlocks for c6607f74): 18 standard + 18 mapping + 3 structuralBridge + 1 reference. Mapping members = 12 non-CTDL CEDS pairings + CIP::SOC + 6 CEDS::CTDL blocks (1 preserved + 5 campaign). Delta vs 260717 (35 members) = the 5 CTDL->CEDS campaign selections. Version-switching = swap a standard's recipe (rebuild) or a pairing's CURRENT selection (pointer flip)."
}
